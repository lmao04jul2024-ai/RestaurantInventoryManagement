import ReviewsPage from '@/components/dashboard/reviews-page';

export const metadata = { title: 'Reviews — Restaurant Manager' };

export default function DashboardReviewsPage() {
  return <ReviewsPage />;
}