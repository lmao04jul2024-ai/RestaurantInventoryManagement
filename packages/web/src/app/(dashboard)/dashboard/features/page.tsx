import FeaturesPage from '@/components/dashboard/features-page';

export const metadata = { title: 'Feature Flags — Restaurant Manager' };

export default function DashboardFeaturesPage() {
  return <FeaturesPage />;
}