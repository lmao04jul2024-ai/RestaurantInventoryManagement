import DashboardIndex from '@/components/dashboard/dashboard-index';

export const metadata = { title: 'Dashboard — Restaurant Manager' };

export default function DashboardHomePage() {
  return <DashboardIndex />;
}
