import InventoryDashboard from '@/components/inventory/inventory-dashboard';

export const metadata = { title: 'Inventory — Restaurant Manager' };

export default function DashboardInventoryPage() {
  return <InventoryDashboard />;
}