import TenantsPage from '@/components/dashboard/tenants-page';

export const metadata = { title: 'Restaurant Settings — Restaurant Manager' };

export default function DashboardTenantsPage() {
  return <TenantsPage />;
}
