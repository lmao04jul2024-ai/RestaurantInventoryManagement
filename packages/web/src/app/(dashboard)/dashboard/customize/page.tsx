import CustomizationPage from '@/components/dashboard/customization-page';

export const metadata = { title: 'Customize — Restaurant Manager' };

export default function DashboardCustomizePage() {
  return <CustomizationPage />;
}