import OrdersDashboard from '@/components/orders/orders-dashboard';

export const metadata = { title: 'Orders — Restaurant Manager' };

export default function DashboardOrdersPage() {
  return <OrdersDashboard />;
}
