import SecurityPage from '@/components/dashboard/security-page';

export const metadata = { title: 'Security — Restaurant Manager' };

export default function DashboardSecurityPage() {
  return <SecurityPage />;
}