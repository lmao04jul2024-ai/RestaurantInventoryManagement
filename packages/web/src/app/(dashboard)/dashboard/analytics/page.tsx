import AnalyticsPage from '@/components/dashboard/analytics-page';

export const metadata = { title: 'Analytics — Restaurant Manager' };

export default function DashboardAnalyticsPage() {
  return <AnalyticsPage />;
}