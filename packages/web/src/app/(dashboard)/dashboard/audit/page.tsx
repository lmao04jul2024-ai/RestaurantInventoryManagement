import AuditPage from '@/components/dashboard/audit-page';

export const metadata = { title: 'Audit Log — Restaurant Manager' };

export default function DashboardAuditPage() {
  return <AuditPage />;
}
